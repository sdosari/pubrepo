---
- name: Demo playbook
  hosts: 192.168.100.118
  remote_user: s

  tasks:
  - name: Copy test file
    ansible.builtin.copy:
      src: /home/s/pt.tar.gz
      dest: /home/s/pt.tar.gz

  - name: Extract foo.tgz into /var/lib/foo
    ansible.builtin.unarchive:
      src: /home/s/pt.tar.gz
      dest: /home/s/
      
